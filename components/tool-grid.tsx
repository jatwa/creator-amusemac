import { tools } from "@/data/content";
export function ToolGrid() { return <div className="grid gap-4 lg:grid-cols-3">{tools.map((tool, index) => <article key={tool.name} className="surface overflow-hidden"><div className={`h-32 bg-gradient-to-br ${tool.accent} to-transparent p-5`}><span className="rounded-full border border-white/15 bg-black/20 px-3 py-1 text-xs text-zinc-300">0{index + 1}</span></div><div className="p-6"><p className="text-xs font-medium uppercase tracking-wider text-lime">{tool.type}</p><h3 className="mt-3 text-2xl font-semibold text-white">{tool.name}</h3><p className="mt-3 text-sm leading-6 text-zinc-400">{tool.description}</p><div className="mt-7 flex items-center justify-between"><span className="text-xs text-zinc-500">{tool.tag}</span><a href="#compare" className="text-sm font-medium text-white hover:text-lime">View tool →</a></div></div></article>)}</div>; }
