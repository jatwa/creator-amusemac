import { comparisons } from "@/data/content";
export function ComparisonList() { return <div className="divide-y divide-line rounded-2xl border border-line bg-panel">{comparisons.map((item) => <article key={item.left} className="grid gap-5 p-6 sm:grid-cols-[180px_1fr_auto] sm:items-center"><div><p className="text-xs uppercase tracking-wider text-zinc-500">{item.category}</p><h3 className="mt-2 font-medium text-white">{item.left} <span className="text-zinc-600">vs</span> {item.right}</h3></div><p className="text-sm leading-6 text-zinc-400">{item.answer}</p><a href="#tools" className="text-sm font-medium text-lime">Compare →</a></article>)}</div>; }
